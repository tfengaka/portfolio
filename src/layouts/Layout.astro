---
import "../styles/global.css";

interface Props {
    title: string;
    description?: string;
}

const {
    title,
    description = "Portfolio of Nguyen Thanh Hoa, Software Engineer.",
} = Astro.props;

const navItems = [
    { name: "About", href: "#about" },
    { name: "Experience", href: "#experience" },
    { name: "Projects", href: "#projects" },
    { name: "Contact", href: "#contact" },
];
---

<!doctype html>
<html lang="en" class="dark">
    <!-- Force dark mode style -->
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content={description} />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/public/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>{title}</title>
    </head>
    <body
        class="bg-[#0B1120] text-slate-400 font-sans min-h-screen flex flex-col"
    >
        <!-- Navbar -->
        <header
            class="fixed top-0 left-0 right-0 z-50 bg-[#0B1120]/80 backdrop-blur-md border-b border-slate-800/50"
        >
            <div
                class="max-w-7xl mx-auto px-6 h-20 flex items-center justify-between"
            >
                <a
                    href="/portfolio"
                    class="text-2xl font-bold text-white tracking-tighter hover:text-accent transition-colors"
                >
                    Dev<span class="text-accent">/</span>
                </a>

                <nav class="hidden md:flex gap-8">
                    {
                        navItems.map((item) => (
                            <a
                                href={item.href}
                                class="text-sm font-medium text-slate-400 hover:text-white transition-colors relative group py-1"
                            >
                                {item.name}
                                <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-accent transition-all duration-300 group-hover:w-full" />
                            </a>
                        ))
                    }
                </nav>
            </div>
        </header>

        <main class="flex-1 w-full pt-20 animate-fade-in px-6">
            <slot />
        </main>

        <footer
            class="py-8 text-center text-xs text-slate-600 border-t border-slate-900 mt-20"
        >
            <p>
                © {new Date().getFullYear()} Nguyen Thanh Hoa | Software Engineer
            </p>
        </footer>
    </body>
</html>
